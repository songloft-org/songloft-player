# DESIGN.md — Apple Human Interface Guidelines（人机界面指南）总结

> 来源：[Apple Human Interface Guidelines](https://developer.apple.com/cn/design/human-interface-guidelines/)
> 整理日期：2026-09-02
> 覆盖平台：iOS、iPadOS、macOS、tvOS、visionOS、watchOS
>
> HIG（Human Interface Guidelines）是 Apple 官方的设计指南，包含帮助开发者为所有 Apple 平台打造出色体验的设计指导与最佳实践。本文档是对 HIG 核心章节的详细总结。

---

## 目录

1. [设计原则（Design Principles）](#1-设计原则design-principles)
2. [设计基础目录（Foundations）](#2-设计基础目录foundations)
3. [颜色（Color）](#3-颜色color)
4. [排版（Typography）](#4-排版typography)
5. [布局（Layout）](#5-布局layout)
6. [材质（Materials）与 Liquid Glass](#6-材质materials与-liquid-glass)
7. [App 图标（App Icons）](#7-app-图标app-icons)
8. [无障碍（Accessibility）](#8-无障碍accessibility)
9. [菜单（Menus）](#9-菜单menus)
10. [输入方式、模式、组件与技术目录](#10-输入方式模式组件与技术目录)
11. [UI 组件设计详解](#11-ui-组件设计详解)
12. [关键规格速查表](#12-关键规格速查表)

---

## 1. 设计原则（Design Principles）

最成功、最持久的设计，建立在对人们如何思考、感受并与世界互动的深刻理解之上。以下八大原则是整个 HIG 的基石，它们不是唯一正确的做法，而是帮助你在相互冲突的优先级之间权衡取舍、做出关键设计决策的工具。

### 1.1 目的（Purpose）—— 创造有意义的东西

设计始于意图。找出对你所服务的人最重要的事情，专注把它们做到极致。

- **创造价值**：在每个开发阶段都问自己——产品是用来做什么的？设计是否服务于这个目的？
- **保持聚焦**：根据人们的使用方式对最重要的功能排序，并把这些功能真正做到出色。用途明确的产品更有效。
- **寻找新的解题方式**：研究现有方案、避免重复造轮子；定义你的产品有何不同，并让设计体现这种差异。

### 1.2 自主（Agency）—— 让人们按自己的方式做事

界面的存在是为了帮助人们达成目标。给予行动自由、告知正在发生什么、让从错误中恢复变得容易。

- **不要挡路**：人们使用产品是为了完成任务，最好的设计是不显眼的——在需要时出现，平时不干扰。
- **给予探索自由**：不要把人锁定在特定流程或模式里；必须有引导式流程时，让人可以轻松跳过或退出。
- **帮助从错误中恢复**：当人们知道可以撤销操作或回到之前状态时，才敢于自由探索。把"容错"内建到设计中。

### 1.3 责任（Responsibility）—— 以人们的最大利益行事

你的工作会影响人们的生活。通过优先考虑安全与隐私、对产品行为保持透明来赢得信任。

- **完全透明**：从第一次交互开始就让用户清楚 App 的意图；请求权限时给出明确理由；收集数据时说明收集什么、如何使用。
- **保护信息安全**：只收集产品运行所必需的数据，并谨慎处理；预见可能的滥用与伤害，提前部署保护措施。

### 1.4 熟悉感（Familiarity）—— 建立在人们已知的基础上

借用人们已经理解的概念（物理世界与其他软件的经验），让他们立刻有"回家"的感觉。

- **使用人们熟悉的概念**：同时借鉴现实世界和其他软件的知识，让界面直观自然。
- **保持视觉与交互一致**：一旦为某个元素确立了行为或外观，就在整个设计中贯彻它。一致性帮助人们更快学习。
- **提供清晰的反馈**：对正在发生的事给出明确信号——控件何时可用、内容何时变化，并用系统模式展示提醒和选项。

### 1.5 灵活（Flexibility）—— 适应多样的情境与需求

人们使用软件的方式和他们本人一样独一无二。支持尽可能多的设备、交互方式和视角。

- **为每个人设计**：考虑使用者的多样性，把无障碍（accessibility）从一开始就作为优先事项，包容性设计能触达最广泛的受众。
- **保留用户的情境**：跨平台和配置变化时，把内容和控件放在一致、可预测的位置，用自然的动画缓解过渡。
- **考虑多种输入方式**：语音、触控、键盘等——让更多人能以最适合他们的方式使用你的产品。
- **带着意图对待每个平台**：为你支持的每个平台投入同样级别的打磨。

### 1.6 简洁（Simplicity）—— 清晰直接

精心设计的体验会去掉不必要的部分，每个元素都要"挣得"自己的位置。

- **只包含必需的**：简洁不等于极简主义。追求聚焦、有用的体验——重要的东西触手可及，其余的退居其后。
- **言简意赅**：找到表达某件事最简单的方式，它往往也是最普适、最有用的。
- **建立层级**：优先使用可识别的控件和一致的结构，帮助人们理解"我在哪、接下来是什么"。

### 1.7 匠心（Craft）—— 在乎每个细节

你的设计反映出你有多在乎。它体现了你为人们提供最佳体验的投入。

- **质量定调**：每个设计决策都要有意识——追求惊艳的视觉、流畅的动画、精确的措辞和考究的音频。
- **实验与迭代**：尽早做原型、尝试新方法、勇于放弃不奏效的方案；在真实场景中测试产品。
- **持续维护匠心**：发布不是终点。让界面跟上最新的平台能力和设计模式。

### 1.8 愉悦（Delight）—— 让它有人情味

人们记住的是产品带给他们的感受。

- **明确你想唤起的情绪**：健身 App 让人振奋、冥想 App 让人平静、游戏让人兴奋——让目标情绪塑造你的设计。
- **创造决定性时刻**：每次交互都是展示软件理念的机会，从按下按钮到错误提示，都可以加入一点反映设计精神的个性。
- **不要把愉悦误当装饰**：人们在完成任务，不要让追求愉悦妨碍产品的核心目的。
- **着眼整体**：愉悦是你投入产品所有心思的总和——行动的自由、探索的安全感、熟悉隐喻的舒适、跨情境切换的灵活。

---

## 2. 设计基础目录（Foundations）

"设计基础"是塑造一切优秀体验的核心概念集合，HIG 在此提供以下主题的详细指导：

| 主题 | 说明 |
|---|---|
| Accessibility（无障碍） | 让所有人都能使用你的产品 |
| App icons（App 图标） | 品牌与体验的门面 |
| Branding（品牌） | 在系统体验中表达品牌 |
| Color（颜色） | 系统颜色、自定义颜色、颜色管理 |
| Dark Mode（深色模式） | 浅色/深色外观适配 |
| Icons（图标） | 界面图标设计 |
| Images（图像） | 照片、插画、图形使用 |
| Immersive experiences（沉浸式体验） | visionOS 沉浸体验 |
| Inclusion（包容性） | 面向多元人群的设计 |
| Layout（布局） | 结构、层级、适配 |
| Materials（材质） | Liquid Glass 与标准材质 |
| Motion（动效） | 动画与过渡 |
| Privacy（隐私） | 隐私设计 |
| Right to left（从右到左） | RTL 语言与布局 |
| SF Symbols | 系统符号库 |
| Spatial layout（空间布局） | visionOS 空间布局 |
| Typography（排版） | 字体、字号、层级 |
| Writing（文案） | 界面文案写作 |

此外，"Getting Started"提供面向各平台的入门设计指南：Designing for iOS / iPadOS / macOS / tvOS / visionOS / watchOS，以及 Designing for games。

---

## 3. 颜色（Color）

系统定义了在各种背景和外观模式下都表现良好、并能自动适应明暗效果（vibrancy）和无障碍设置的颜色。使用系统颜色是让体验在设备上"感觉像原生的"的便捷方式；你也可以用自定义颜色增强视觉体验、表达产品的独特个性。

### 3.1 最佳实践

- **避免同一颜色代表不同含义**：在界面中保持颜色使用一致，尤其在用颜色传达状态、可交互性等信息时。例如用品牌色表示无边框按钮可交互，就不要再用相同/相似颜色去样式化非交互文本。
- **确保颜色在浅色、深色和高对比度环境下都能正常工作**：系统颜色会随系统外观微妙调整以保证对比度；开启"增强对比度"后差异更明显。尽量使用系统颜色（已包含所有变体）；自定义颜色必须提供浅色、深色变体，并为每个变体提供对比度显著更高的增强对比度版本。即使你的 App 只以一种外观模式发布，也应提供浅色和深色两套颜色，以支持 Liquid Glass 在这些情境下的自适应。
- **在多种光照条件下测试**：明亮的户外颜色显得更暗更灰；昏暗环境中颜色显得更亮更饱和。在 visionOS 中，颜色还会受用户物理环境（墙面、物体反光）影响。
- **在不同设备上测试**：如 iPhone/iPad/Mac 的 True Tone 显示会用环境光传感器调整白点；tvOS App 应在多个品牌、多种显示设置的电视上测试；也可在 Mac 上用不同颜色配置文件（P3、sRGB）测试。
- **注意插图与半透明对邻近颜色的影响**：如 Maps 在地图模式用浅色方案、卫星模式切换为深色方案；颜色置于半透明元素（如工具栏）后面或应用其上时观感也会变化。
- **让用户选色时优先使用系统颜色选择器**：提供一致体验，且用户可保存一组跨应用可用的颜色。

### 3.2 包容性颜色（Inclusive color）

- **不要仅靠颜色区分对象、指示可交互性或传达关键信息**：同时提供替代方式（文本标签、图形形状），帮助色觉障碍用户理解。
- **避免使用让人难以感知内容的颜色**：对比度不足会让图标和文字与背景融为一体。
- **考虑颜色在不同国家文化中的感知差异**：如红色在某些文化代表危险，在其他文化却有积极含义。

### 3.3 系统颜色（System colors）

- **不要在代码中硬编码系统颜色值**：文档中的颜色值仅供设计参考，实际值会随版本与环境变化。通过 API（如 SwiftUI 的 `Color`）应用系统颜色。
- iOS/iPadOS/macOS/visionOS 定义了**动态系统颜色**集合，与标准 UI 组件的配色方案匹配，自动适应浅色/深色。每种动态颜色按**用途**（语义）定义，而非按外观或色值定义（如视图背景层级、标签、链接、分隔线）。
- **不要重新定义动态系统颜色的语义**：例如不要把 `separator` 颜色当文字颜色用，也不要把 `secondaryLabel` 当背景颜色用。

### 3.4 Liquid Glass 颜色

- Liquid Glass 默认没有固有颜色，而是呈现其正后方内容的颜色。你可以给部分 Liquid Glass 元素着色，使其看起来像彩色玻璃——适合突出特定控件（如主要行动按钮，即系统的 prominent 按钮样式）。
- 对于工具栏、标签栏等较小元素，系统可根据底层内容在浅色与深色外观间自适应；这些元素上的符号和文本默认采用单色方案（底层内容亮则变深，暗则变浅）。侧边栏等较大元素中的 Liquid Glass 更不透明，以保证复杂背景上的可读性。
- **谨慎地在 Liquid Glass 上使用颜色**：只保留给真正需要强调的元素（状态指示器、主要操作）。强调主要操作时把颜色用于背景而非符号/文本（如 Done 按钮使用 App 强调色背景）。不要给多个控件的背景都上色。
- **背景色彩丰富时避免控件标签使用相似颜色**：过多颜色会令人眼花缭乱、降低可读性。背景丰富时优先让工具栏和标签栏保持单色外观；以单色内容为主的 App，把品牌色作为 App 强调色是很好的品牌化手段。
- **注意内容层中颜色的位置**：避免内容层与控件上相似颜色的重叠，确保可滚动内容的默认/静止状态（如屏幕顶部）保持清晰可读。

### 3.5 颜色管理

- **给图像应用颜色配置文件**：确保颜色在不同显示器上按预期呈现；sRGB 在大多数显示器上都能产生准确颜色。
- **在兼容显示器上用广色域增强视觉体验**：P3 色域比 sRGB 更丰富、更饱和。适用时使用 Display P3 配置文件（每通道 16 位）并以 PNG 导出图像；设计广色域图像本身也需要广色域显示器。
- **必要时提供特定色域的图像/颜色变体**：P3 图像在 sRGB 显示器上通常没问题，但两个非常接近的 P3 颜色在 sRGB 上可能难以区分，P3 渐变可能被裁剪。可在 Xcode asset catalog 中为每种色域提供不同版本。

### 3.6 平台注意事项

- **iOS/iPadOS**：定义两套动态背景色——system 与 grouped，各含 primary/secondary/tertiary 三个层级变体。分组表格视图用 grouped 组（`systemGroupedBackground` 等），否则用 system 组（`systemBackground` 等）。层级用法：Primary 用于整体视图，Secondary 用于整体视图内的内容分组，Tertiary 用于次级元素内的再分组。前景内容定义了 Label、Secondary/Tertiary/Quaternary label、Placeholder text、Separator、Opaque separator、Link 等动态颜色。
- **macOS**：提供数十种语义化动态颜色（label、link、separator、selected content background、window background 等）。自 macOS 11 起可指定 App 强调色（accent color）来自定义按钮、选择高亮和侧边栏图标——仅当系统"强调色"设置为"多色"时生效；若用户选择了其他强调色，系统会用用户的颜色覆盖你的设置（固定颜色的侧边栏图标除外）。
- **tvOS**：考虑选择与 App 标识协调的有限色板；避免只用颜色指示焦点（用微妙的缩放和响应式动画来表达）。
- **visionOS**：谨慎使用颜色，尤其在玻璃上——标准窗口使用系统玻璃材质，物理环境的光和物体会透过来影响可读性；颜色优先用于粗体文字和大面积区域；在完全沉浸体验中保持亮度平衡（避免在很暗的背景上显示明亮物体，尤其是闪烁或移动的）。
- **watchOS**：背景颜色用于支持内容或提供附加信息（如 Activity 圆环的背景匹配圆环颜色），不要只作视觉装饰；避免在长时间停留屏幕的视图中使用全屏背景色；系统可对图形复杂功能使用着色模式而非全彩。

### 3.7 规格摘要

- 系统颜色（SwiftUI）：Red、Orange、Yellow、Green、Mint、Teal、Cyan、Blue、Indigo、Purple、Pink、Brown，每种都有浅色/深色/高对比度变体。visionOS 使用深色变体值。
- iOS/iPadOS 另有 systemGray 1–6 六级灰阶（SwiftUI 等价物为 `gray`）。
- 支持的颜色空间：sRGB（彩色）、Gray Gamma 2.2（灰度）、Display P3（广色域，限 iOS、iPadOS、macOS、tvOS、watchOS）。

---

## 4. 排版（Typography）

### 4.1 确保可读性（Ensuring legibility）

- **使用大多数人能轻松阅读的字号**：遵循各平台推荐的默认与最小字号（自定义字体也一样）。细字重（thin）的可读性更差，需要更大的字号。
- 各平台默认/最小字号：

| 平台 | 默认字号 | 最小字号 |
|---|---|---|
| iOS / iPadOS | 17 pt | 11 pt |
| macOS | 13 pt | 10 pt |
| tvOS | 29 pt | 23 pt |
| visionOS | 17 pt | 12 pt |
| watchOS | 16 pt | 12 pt |

- **在不同情境下测试可读性**：必要时使用更大的字号、提高文字/背景对比度，或改用为可读性优化的字体（如系统字体）。
- **避免细字重**：优先 Regular、Medium、Semibold、Bold；避免 Ultralight、Thin、Light（尤其小字号时难以辨认）。

### 4.2 传达层级（Conveying hierarchy）

- 按需调整字重、字号、颜色来强调重要信息、帮助人们理解层级；用户调整字号时保持相对层级和视觉区分度。
- **尽量减少使用的字体种类**：字体过多会模糊信息层级、损害可读性，还让界面显得不协调。
- **响应字号变化时优先重要内容**：用户调大字号是想让他们关心的内容更易读，而不是让屏幕上每个字都变大（如增大正文字号时 Tab 标题不必跟着变大）。

### 4.3 使用系统字体（Using system fonts）

Apple 提供两大字体家族，支持极广的字重、字号、样式和语言：

- **San Francisco（SF）**：无衬线字体家族，包括 SF Pro、SF Compact、SF Arabic、SF Armenian、SF Georgian、SF Hebrew、SF Mono 等；还有 rounded（圆体）变体，可配合圆润的 UI 元素或提供另一种字体风格。
- **New York（NY）**：衬线字体家族，可独立使用也可与 SF 搭配。
- 系统字体以**可变字体（variable font）**格式提供，单文件包含多种样式，支持样式间插值。支持**动态光学尺寸（dynamic optical sizes）**：把离散的光学尺寸（如 Text/Display）和字重融合为单一连续设计，让系统为每个字形插值出精确适配点尺寸的结构。
- 系统字体提供 Ultralight 到 Black 的多种字重；SF 还提供 Condensed、Expanded 等宽度。SF Symbols 使用等价字重，可实现符号与相邻文字的精确字重匹配。
- **文本样式（text styles）**：系统定义的一组排版属性（字重 + 字号 + 行距的组合），构成可用的排版层级，并让文本在用户改变系统字号或无障碍设置（如 Larger Text）时按比例缩放。
- **优先使用内建文本样式**：方便、一致，且自动支持 Dynamic Type 和更大的无障碍字号。
- 必要时用 **symbolic traits** 修改文本样式（如 bold trait 加重字重、调整行距 leading）：宽栏长文适合宽松行距；高度受限区域（如列表行）可收紧行距，但三行以上文字即使高度受限也应避免过紧行距。
- 通过 `Font.Design` 常量访问系统字体，**不要在 App 中嵌入系统字体**：`Font.Design.default` 得到系统字体，`Font.Design.serif` 得到 New York。
- 在界面模型稿中可能需要手动调整 tracking（字间距）；运行中的 App 由系统动态调整。

### 4.4 使用自定义字体（Using custom fonts）

- **确保自定义字体可读**：遵循各样式/字重的推荐最小字号。
- **为自定义字体实现无障碍特性**：系统字体自动支持 Dynamic Type、Bold Text 等无障碍特性，自定义字体必须实现同样的行为。Unity 游戏可用 Apple 的 Unity 插件（plugins）支持 Dynamic Type。

### 4.5 支持 Dynamic Type

Dynamic Type 是 iOS、iPadOS、tvOS、visionOS、watchOS 的系统级功能，让用户调整可见文字大小以保证可读与舒适。

- **确保布局适应所有字号**：在"设置 > 辅助功能 > 显示与文字大小 > 更大字体"中开启更大无障碍字号，验证 App 依然舒适可读。
- **字号增大时同步放大有意义的界面图标**：使用 SF Symbols 可自动随 Dynamic Type 缩放。
- **尽量减少文字截断**：在最大无障碍字号下尽量显示与最大标准字号同样多的有用文本；可滚动区域避免截断（除非能打开单独视图读完整内容）；允许 label 使用足够多行数。
- **大字号时调整布局**：水平受限的上下文中，行内元素（图标、时间戳）和容器边界会挤压文字——考虑改用文字在上、次要元素在下的堆叠布局；大字号下减少多栏文本的栏数。
- **无论字号如何都保持一致的信息层级**：即使字号很大，主要元素仍应保持在视图顶部附近。

### 4.6 平台注意事项

- **iOS / iPadOS**：系统字体为 SF Pro，也可使用 NY。
- **macOS**：系统字体为 SF Pro；NY 可用于 Mac Catalyst 应用；**macOS 不支持 Dynamic Type**。需要匹配标准控件文本时使用动态系统字体变体（control content、label、menu、menu bar、message、palette、title、tool tips、document text 等）。
- **tvOS**：系统字体为 SF Pro，也可使用 NY。
- **visionOS**：系统字体为 SF Pro；使用 NY 需指定具体样式。visionOS 使用更粗的 Dynamic Type body/title 样式，并新增 Extra Large Title 1/2 用于宽幅编辑式布局。空间排版要点：
  - 优先 2D 文字——视觉深度越大越难阅读；需要阅读的内容避免使用 3D 文字。
  - 缩放文字时保证观感和可读性。
  - 最大化文字与背景对比度（默认白色文字在玻璃材质上对比良好）；无背景的文字考虑加粗而非加阴影（空间中没有可投射准确阴影的表面）。
  - 让文字始终面向用户（billboarding）——如 3D 物体标签应随用户移动绕 y 轴旋转，保持基线与视线垂直。
- **watchOS**：系统字体为 SF Compact，也可使用 NY；复杂功能（complications）使用 SF Compact Rounded。

### 4.7 规格摘要（内建文本样式）

**macOS 内建文本样式**：

| 样式 | 字重 | 字号 | 行高 | 强调字重 |
|---|---|---|---|---|
| Large Title | Regular | 26 pt | 32 pt | Bold |
| Title 1 | Regular | 22 pt | 26 pt | Bold |
| Title 2 | Regular | 17 pt | 22 pt | Bold |
| Title 3 | Regular | 15 pt | 20 pt | Semibold |
| Headline | Bold | 13 pt | 16 pt | Heavy |
| Body | Regular | 13 pt | 16 pt | Semibold |
| Callout | Regular | 12 pt | 15 pt | Semibold |
| Subheadline | Regular | 11 pt | 14 pt | Semibold |
| Footnote | Regular | 10 pt | 13 pt | Semibold |
| Caption 1 | Regular | 10 pt | 13 pt | Medium |
| Caption 2 | Medium | 10 pt | 13 pt | Semibold |

**tvOS 内建文本样式**（节选）：Title 1 = Medium 76 pt、Title 2 = 57 pt、Title 3 = 48 pt、Headline = 38 pt、Body = 29 pt、Caption 1 = 25 pt、Caption 2 = 23 pt。

**Tracking（字间距）**：系统字体按字号动态调整 tracking——小字号时为正（字距放宽），约 17–20 pt 区间最紧（如 17 pt 时约 -0.43 pt），24 pt 以上逐渐回正，80 pt 及以上为 0。制作模型稿时需手动按此规律调整。

---

## 5. 布局（Layout）

App 的布局从打开那一刻就帮人们在内容中找到方向。人们期待控件与内容之间存在熟悉的关系——利用好这一点，让 App 在平台上"感觉像原生的"。Apple 提供模板、参考线和其他资源（Apple Design Resources）帮助集成技术、设计跨平台应用与游戏。

### 5.1 最佳实践

- **把相关项目分组**：用负空间、背景形状、颜色、材质或分隔线表明元素间的关联、把信息分成独立区域，同时确保内容和控件保持清晰可辨。
- **让关键信息易于发现**：给它足够的空间，不要用非必要的细节把它挤没；次要信息可以放在窗口其他位置或额外视图里。
- **让内容延伸填满屏幕/窗口**：背景和全屏插图延伸到显示边缘；可滚动布局应延伸到屏幕底部和两侧。侧边栏、标签栏等控件与导航组件浮在内容之上而非同一平面，布局必须考虑这一点。
- 当内容不占满窗口时，用**背景延伸视图（background extension view）**在屏幕两侧（如侧边栏或检查器下方）提供控件层之后的内容外观。

### 5.2 视觉层级（Visual hierarchy）

- **区分控件与内容**：利用 Liquid Glass 材质为控件提供跨 iOS/iPadOS/macOS 一致的独特外观；用滚动边缘效果（scroll edge effect）代替背景，提供内容与控件区域之间的过渡。
- **用位置传达相对重要性**：人们通常按阅读顺序浏览（从上到下、从前缘到后缘）——最重要的项目放在窗口/屏幕的顶部和前缘附近。注意阅读顺序因语言而异，要考虑从右到左（RTL）语言。
- **对齐组件**：对齐让应用整洁有序，帮助人们在滚动或移动视线时跟踪内容、理解信息层级。
- **利用渐进式披露（progressive disclosure）**：无法一次显示大集合的全部条目时，提示还有更多内容（如展开控件、露出部分条目暗示可滚动）。
- **给控件足够的空间并按逻辑分区**：不相关的控件靠得太近会难以区分、难以理解。

### 5.3 适配性（Adaptability）

每个应用和游戏都需要在设备或系统情境变化时做出适应。iOS/iPadOS/tvOS/visionOS 用 traits 描述影响界面呈现的设备环境变化；使用 SwiftUI 或 Auto Layout 可以让界面动态适配。

需要处理的常见变化：

- 不同的屏幕尺寸、分辨率、色域
- 不同的设备方向（竖屏/横屏）
- 灵动岛（Dynamic Island）、相机控制键等系统特性
- 外接显示器、Display Zoom、iPad 可调整窗口大小
- Dynamic Type 文字大小变化
- 基于语言环境的国际化特性：LTR/RTL 布局、日期/时间/数字格式、字体变体、文本长度

要点：

- **布局优雅地适应情境变化，同时保持可识别的一致性**：尊重系统定义的安全区（safe area）、边距和参考线，并用布局修饰符微调视图位置。
- **准备好应对文字大小变化**：支持 Dynamic Type，让应用正确响应用户的文字大小调整。
- **在多台设备、多种方向、本地化和字号下预览**：先测最大和最小布局版本；用模拟器检查裁剪和布局问题。
- **必要时缩放插图而非改变宽高比**：宽高比变化时用缩放保证重要视觉内容可见。

### 5.4 参考线与安全区（Guides and safe areas）

- **布局参考线（layout guide）**定义矩形区域帮助定位、对齐和留白；系统预定义参考线便于应用标准边距、限制文本宽度以获得最佳可读性。
- **安全区（safe area）**定义视图中未被工具栏、标签栏等遮挡的区域，对避开灵动岛、Mac 摄像头凸台等至关重要。
- **尊重每个平台的关键显示与系统特性**：不适配会让应用"不像属于这个平台"且更难用。

### 5.5 不同分辨率屏幕的布局规格（Specifications）

HIG 的核心思路是：**不要按具体分辨率做布局，而是围绕"点（point）+ size class + 安全区"构建自适应布局**，再用下面的设备规格表做校验。以下数据来自 Layout 章节官方规格表（2025 年 9 月更新，含 iPhone 17 系列）。

#### 5.5.1 尺寸类别（Size classes）

尺寸类别是 **regular（常规）**或 **compact（紧凑）**两个值：regular 对应更大的屏幕或横屏方向，compact 对应更小的屏幕或竖屏方向。布局应针对尺寸类别组合设计，而非具体设备型号。

**iPad 全系列**（iPad Pro / iPad Air / iPad / iPad mini，无论尺寸）：

| 方向 | 宽度类别 | 高度类别 |
|---|---|---|
| 竖屏 | Regular | Regular |
| 横屏 | Regular | Regular |

**iPhone 尺寸类别规律**：

- 竖屏：所有机型均为 **Compact 宽度 + Regular 高度**。
- 横屏：
  - **Plus / Max 级大屏机型**（iPhone 6 Plus 起，含 iPhone Air）→ Regular 宽度 + Compact 高度；
  - **其余机型**（包括 iPhone 8 Plus 及以下、各 Pro / 标准 / mini / SE 机型）→ Compact 宽度 + Compact 高度。

> 注：上表中的缩放系数均为 UIKit 的 `scale` 值，可能与原生缩放系数（`nativeScale`）不同。

#### 5.5.2 iPhone 屏幕尺寸（竖屏，点/像素）

| 机型 | 逻辑尺寸 (pt) | 像素 | 缩放 |
|---|---|---|---|
| iPhone 17 Pro Max / iPhone 16 Pro Max | 440×956 | 1320×2868 | @3x |
| iPhone 15 Pro Max / iPhone 15 Plus | 430×932 | 1290×2796 | @3x |
| iPhone Air | 420×912 | 1260×2736 | @3x |
| iPhone 14 Plus | 428×926 | 1284×2778 | @3x |
| iPhone 13 Pro Max / iPhone 12 Pro Max | 428×926 | 1284×2778 | @3x |
| iPhone 11 Pro Max / iPhone XS Max | 414×896 | 1242×2688 | @3x |
| iPhone 11 / iPhone XR | 414×896 | 828×1792 | @2x |
| iPhone 8 Plus / 7 Plus / 6s Plus / 6 Plus | 414×736 | 1080×1920 | @3x |
| iPhone 17 Pro / iPhone 17 / iPhone 16 Pro | 402×874 | 1206×2622 | @3x |
| iPhone 16 / iPhone 15 Pro / iPhone 15 / iPhone 14 Pro | 393×852 | 1179×2556 | @3x |
| iPhone 16e / iPhone 14 / iPhone 13 Pro / iPhone 13 / iPhone 12 Pro / iPhone 12 | 390×844 | 1170×2532 | @3x |
| iPhone 11 Pro / iPhone XS / iPhone X | 375×812 | 1125×2436 | @3x |
| iPhone 8 / 7 / 6s / 6 / SE（4.7 英寸） | 375×667 | 750×1334 | @2x |
| iPhone 13 mini / iPhone 12 mini | 360×780 | 1080×2340 | @3x |
| iPhone SE（4 英寸）/ iPod touch（第 5 代起） | 320×568 | 640×1136 | @2x |

**关键推论**：iPhone 竖屏的宽度实际上只有 **320 / 360 / 375 / 390 / 393 / 402 / 414 / 420 / 428 / 430 / 440 pt** 这十来档——布局设计只需覆盖这少数几个宽度断点即可兼容所有机型。

#### 5.5.3 iPad 屏幕尺寸（竖屏，点/像素）

| 机型 | 逻辑尺寸 (pt) | 像素 |
|---|---|---|
| iPad Pro 13 英寸 | 1032×1376 | 2064×2752 @2x |
| iPad Pro 12.9 英寸 / iPad Air 13 英寸 | 1024×1366 | 2048×2732 @2x |
| iPad Pro 11 英寸（第 5、6 代） | 834×1210 | 1668×2420 @2x |
| iPad Pro 11 英寸（第 1–4 代） | 834×1194 | 1668×2388 @2x |
| iPad Pro 10.5 英寸 / iPad Air 10.5 英寸 | 834×1112 | 1668×2224 @2x |
| iPad Air 11 英寸 / iPad 11 英寸 | 820×1180 | 1640×2360 @2x |
| iPad Air 10.9 英寸 | 820×1180 | 1640×2360 @2x |
| iPad 10.2 英寸 | 810×1080 | 1620×2160 @2x |
| iPad Pro 9.7 英寸 / iPad Air 9.7 英寸 / iPad 9.7 英寸 | 768×1024 | 1536×2048 @2x |
| iPad mini 8.3 英寸 | 744×1133 | 1488×2266 @2x |
| iPad mini 7.9 英寸 | 768×1024 | 1536×2048 @2x |

注意：iPadOS 窗口可自由缩放（见 5.6 平台注意事项），实际窗口尺寸范围远大于整屏尺寸，需按"从最小到最大"全范围测试。

#### 5.5.4 watchOS 屏幕尺寸（像素）

| 系列 | 尺寸 | 宽 (px) | 高 (px) |
|---|---|---|---|
| Apple Watch Ultra（第 3 代） | 49mm | 422 | 514 |
| Series 10、11 | 42mm | 374 | 446 |
| Series 10、11 | 46mm | 416 | 496 |
| Apple Watch Ultra（第 1、2 代） | 49mm | 410 | 502 |
| Series 7、8、9 | 41mm | 352 | 430 |
| Series 7、8、9 | 45mm | 396 | 484 |
| Series 4、5、6、SE（各代） | 40mm | 324 | 394 |
| Series 4、5、6、SE（各代） | 44mm | 368 | 448 |
| Series 1、2、3 | 38mm | 272 | 340 |
| Series 1、2、3 | 42mm | 312 | 390 |

#### 5.5.5 其他平台的分辨率处理

- **macOS**：HIG 不提供固定分辨率规格表——Mac 窗口可自由调整大小，布局指南以窗口行为为准（避免控件贴窗口底部、避开摄像头凸台、支持窗口缩放）。
- **tvOS**：布局**不随电视尺寸自动适配**，所有显示器呈现同一界面；必须遵守安全区（上下 60 pt、左右 80 pt）并考虑多种电视尺寸。
- **visionOS**：窗口在空间中可沿 z 轴移动，系统会**自动缩放窗口**；布局指南以窗口内边距、控件间距（按钮中心 ≥60 pt）和视野（field of view）为准，而非像素分辨率。

### 5.6 平台注意事项

**iOS**

- 尽量同时支持竖屏和横屏；只支持单一方向时不必提示用户旋转（用户会自己尝试）；只支持横屏时确保左右旋转都表现良好。
- 游戏优先全屏（full-bleed）界面，同时适配圆角、传感器壳、灵动岛。
- **避免全宽按钮**：尊重系统边距、从屏幕边缘内缩；必须全宽时与硬件曲率和相邻安全区协调。
- 仅在隐藏状态栏能增值时（如游戏、媒体）才隐藏它。

**iPadOS**

- 窗口可自由缩放到最小宽高（类似 macOS），设计必须考虑全范围的窗口尺寸。
- **尽可能推迟切换到紧凑视图**：先设计全屏视图，只有完整布局放不下时才切换；复杂布局（如 Split view）优先在变窄时隐藏第三列（如检查器）。
- 在系统提供的常见尺寸（半屏、三分之一、四分之一屏）下测试布局，最小化意外的 UI 变化。
- 考虑**可转换标签栏（convertible tab bar）**：在侧边栏和标签栏之间切换，随视图宽度自动调整呈现。

**macOS**

- 避免把控件或关键信息放在窗口底部（人们常把窗口底边移出屏幕）。
- 避免把内容显示在窗口顶部的摄像头凸台区域内。

**tvOS**

- 布局不像 iPhone/iPad 那样自动适配屏幕尺寸——所有显示器上显示相同界面，需精心设计以应对多种电视尺寸。
- **遵守屏幕安全区**：主要内容距上下边缘 60 pt、距左右 80 pt；只允许部分离屏内容和故意流出的元素出现在此区域外。
- 可聚焦元素之间保留足够间距（元素聚焦时会放大）。
- 网格布局：使用一致的间距；带标题的行增加额外垂直间距；让部分隐藏的离屏内容在屏幕两侧保持对称宽度。

**visionOS**

- 考虑把最重要的内容和控件居中（大窗口中更易发现和交互）。
- 窗口内容保持在窗口边界内——系统在窗口边界外显示窗口控件（Share 菜单在上方，缩放/移动/关闭控件在下方）。
- 窗口外的附加控件用 **ornament**（如工具栏、标签栏）。
- 交互组件之间留出足够空间以便注视识别：按钮中心间距至少 60 pt。
- 注意：标准窗口中沿 z 轴延伸过远的内容会被系统裁剪。

**watchOS**

- 内容从屏幕一端延伸到另一端，利用表圈作为天然内边距，尽量减少元素间留白。
- 一行最多两三个控件：一般不超过 3 个图形按钮或 2 个文字按钮；文字按钮通常全宽更佳。
- 在可能要展示给他人的视图（如图片、二维码）中支持自动旋转（转腕时）。

---

## 6. 材质（Materials）与 Liquid Glass

材质帮助在视觉上把前景元素（文本、控件）与背景元素（内容、纯色）区分开。通过让颜色从背景透到前景，材质建立视觉层级，帮助人们保持方向感。

Apple 平台有两类材质：**Liquid Glass** 与**标准材质（standard materials）**。Liquid Glass 是统一各平台设计语言的动态材质，让你呈现控件与导航而不遮挡底层内容；标准材质则用于内容层内部的视觉区分。

### 6.1 Liquid Glass

Liquid Glass 为控件和导航元素（如标签栏、侧边栏）形成一个独立的功能层，漂浮在内容层之上，在功能元素与内容之间建立清晰的视觉层级。它让内容从这些元素下方滚动、透出，赋予界面动感与深度，同时保持控件和导航的可读性。

- **不要在内容层使用 Liquid Glass**：它最适合区分交互元素与内容；放进内容层会带来不必要的复杂性和混乱的视觉层级。内容层元素（如 App 背景）改用标准材质。例外：内容层中带瞬时交互元素的控件（如 Slider、Toggle）在被激活时会呈现 Liquid Glass 外观以强调可交互性。
- **克制使用 Liquid Glass 效果**：系统框架的标准组件会自动获得该材质的外观和行为；若给自定义控件应用此效果，务必克制——过度使用会分散对底层内容的注意力。把这些效果限制在最重要的功能元素上。
- **只在视觉丰富的背景上使用 clear 变体**：Liquid Glass 提供两个变体——regular 和 clear，会响应某些系统设置（如用户偏好的 Liquid Glass 外观、降低透明度/增强对比度设置）而变化：
  - **regular 变体**：模糊并调整背景内容的亮度以保持可读性；滚动边缘效果进一步增强可读性。大多数系统组件使用此变体。背景可能造成可读性问题、或组件文本量大（提醒框、侧边栏、弹窗）时使用。
  - **clear 变体**：高度透明，适合优先呈现底层内容、让视觉丰富的背景元素保持突出。用于浮在媒体背景（照片、视频）上的组件，营造更沉浸的内容体验。
- 为保证 clear Liquid Glass 组件的对比度与可读性，决定是否在其后添加**调光层（dimming layer）**：
  - 底层内容明亮时，考虑添加 35% 不透明度的深色调光层。
  - 底层内容足够暗，或使用自带调光层的标准媒体播放控件（AVKit）时，无需添加。

### 6.2 标准材质（Standard materials）

- 用标准材质和效果（如 `UIBlurEffect`、`UIVibrancyEffect`、`NSVisualEffectView.BlendingMode`）在 Liquid Glass 之下的内容层传达结构感。
- **按语义含义和推荐用途选择材质**，而非按它赋予界面的表观颜色——系统设置可能改变其外观。把材质/明暗样式匹配到具体用例。
- 在材质之上使用**明暗颜色（vibrant colors）**确保可读性：使用系统定义的明暗颜色，无需担心在不同上下文中显得过暗、过亮、过饱和或对比度低。
- 选择材质结合模糊和明暗效果时考虑对比度与视觉分离：
  - **较厚（thicker）材质**更不透明，为文本等精细特征元素提供更好的对比度。
  - **较薄（thinner）材质**更透明，通过让用户看到背景内容来帮助保持情境。

### 6.3 平台注意事项

**iOS / iPadOS**：除 Liquid Glass 外，仍提供四种标准材质——ultra-thin、thin、regular（默认）、thick，用于内容层的视觉区分。还定义了与每种材质配合的明暗颜色（labels、fills、separators）：label/fill 有多个明暗等级（default 对比最高，quaternary 最低），separator 只有一级。避免在 thin/ultraThin 上用 quaternary（对比太低）。

**macOS**：提供多种有特定用途的标准材质和明暗版本；选择何时在自定义视图中允许明暗效果；选择与界面设计互补的背景混合模式（behind window / within window）。

**tvOS**：Liquid Glass 出现在导航元素和系统体验（Top Shelf、控制中心）中，图像视图和按钮获得焦点时也会采用。标准材质按厚度区分：

| 材质 | 推荐用途 |
|---|---|
| ultraThin | 需要浅色方案的全屏视图 |
| thin | 部分遮挡内容、需浅色方案的叠加视图 |
| regular | 部分遮挡内容的叠加视图 |
| thick | 部分遮挡内容、需深色方案的叠加视图 |

**visionOS**：窗口通常使用不可修改的系统材质 **glass**，让光、环境、虚拟内容和物理环境透出，帮助用户保持定位。glass 是自适应材质，限制背景颜色信息范围以持续提供对比——**随物理环境变亮或变暗**（注意：visionOS 没有独立的深色模式设置，玻璃自动适应其后方物体的亮度）。

- 窗口内优先**半透明而非不透明颜色**——不透明区域会遮挡视野、让人有压迫感。
- 自定义组件可用系统材质：thin 吸引对交互元素（按钮、选中项）的注意；regular 帮助视觉分区（侧边栏、分组表格）；thick 在 regular 背景上创建仍清晰可辨的深色元素。
- visionOS 用明暗效果（vibrancy）提升文本/符号/填充的可读性，定义三级明暗值：`label`（标准文本）、`secondaryLabel`（脚注、副标题）、`tertiaryLabel`（非活跃元素，低可读性需求时用）。

**watchOS**：用材质在全屏模态视图中提供情境——模态视图在 watchOS 很常见，材质层的对比帮助定位、区分控件和系统元素；默认提供的模态弹窗材质背景不要移除或替换。

---

## 7. App 图标（App Icons）

App 图标是品牌与用户体验的关键部分：出现在主屏幕及系统各处（搜索结果、通知、系统设置、分享表单）。优秀的图标在所有 Apple 平台上清晰、一致地传达应用身份。

### 7.1 分层设计（Layer design）

分层图标能营造深度与生命力，各平台系统会对图层应用响应环境和交互的视觉效果。

- **iOS / iPadOS / macOS / watchOS**：一个背景层 + 一个或多个前景层，呈现 Liquid Glass 属性（镜面高光、折射、半透明）。效果随图标尺寸自动调整、跨平台一致，不同系统版本间可能不同。
- **tvOS**：2–5 层，聚焦时图标抬升到前景、轻微摇曳、表面被照亮；层间分离与透明度在视差效果中产生深度感。
- **visionOS**：背景层 + 一两个上层图层，形成观看时微妙扩张的三维物体；系统用阴影表达层间深度、利用上层 alpha 通道创造浮雕外观。

工具：用设计工具制作前景层后，iOS/iPadOS/macOS/watchOS 图标导入 **Icon Composer**（随 Xcode 提供）——可定义背景层、调整前景层位置、应用视觉效果、标注 default/dark/mono 外观变体、跨系统版本预览、导出。tvOS 和 visionOS 直接在 Xcode 的 image stack 中添加图层（可配合 Parallax Previewer/Exporter 插件）。

图层制作要点：

- 前景层**优先清晰定义的边缘**（避免柔化/羽化），让系统绘制的高光和阴影效果最佳。
- 前景层**变化不透明度**增强深度与活力（如 Photos 图标把中心元素拆成多个半透明层）。
- 设计**既突出又衬托前景的背景**：渐变背景要对系统光效响应良好；导入的背景层必须满版（full-bleed）不透明。
- **导入 Icon Composer 时优先矢量图形**（SVG、PDF 可无损缩放）；网格渐变和位图优先 PNG（无损）。

### 7.2 图标形状（Icon shape）

- iOS/iPadOS/macOS：方形，系统施加遮罩产生与系统其他圆角界面和设备边框曲率精确匹配的圆角。
- tvOS：横向矩形，同样是同心圆角。
- visionOS / watchOS：方形，系统施加圆形遮罩。
- **提供形状恰当、未遮罩的图层**：系统遮罩所有图层边缘形成最终形状。预定义遮罩会损害镜面高光效果、让边缘显得锯齿。
- **主体内容居中**，避免系统调整圆角或遮罩时被裁剪；用 Apple Design Resources 中的图标生产模板网格辅助定位。

### 7.3 设计（Design）

- **拥抱简洁**：简单图标最易识别；找到捕捉应用精髓的核心概念，用最少的形状简单独特地表达。背景用纯色或渐变，不必填满整个画布。
- **跨平台视觉一致**：帮助人们快速找到应用、避免误认成多个 App。
- 考虑基于**填充、重叠的形状**设计：前景中重叠的实心形状（配合透明度和模糊）能带来深度感。
- **仅在品牌必需时才包含文字**：图标中的文字不支持无障碍和本地化、往往太小难读；不要放 "Watch"/"Play"/"New" 这类非必要词语。
- **优先插画而非照片**，避免复刻 UI 组件；避免极细线重和尖锐转角（小尺寸下会失去细节）。
- **不要复制 Apple 硬件产品形象**（受版权保护）。

### 7.4 视觉效果

- **让系统处理模糊等视觉效果**：系统动态应用高光、阴影、折射等效果；自定义静态效果会干扰且显得死板。如果一定要加自定义效果，需有意图地加、并在 Icon Composer/模拟器/真机上仔细测试。
- **创建图层分组**以把效果应用到多个图层：Icon Composer 为分组提供额外的 Liquid Glass 效果定制选项（镜面高光、折射、半透明）。

### 7.5 外观变体（Appearances）

- iOS/iPadOS/macOS 主屏幕图标可选 default、dark、clear、tinted 外观；你可为每种外观设计变体，未提供的由系统自动生成。
- **跨外观保持图标特征一致**：不要在变体间替换元素（会让人觉得找不到应用了）。
- **深色/着色图标与系统图标并排时协调**：深色图标更柔和，clear 和 tinted 更甚。
- 用浅色图标作为深色图标的基础：选择互补颜色、避免过亮图像；彩色背景在深色图标中通常对比最好。
- **考虑提供备用图标**（iOS、iPadOS、tvOS 及兼容的 visionOS 应用）：如体育应用提供不同球队图标；每个备用图标都必须与内容紧密相关、不能被误认为其他 App（备用图标也需要各自的 dark、clear、tinted 变体，且都需通过应用审核）。

### 7.6 平台注意事项

- **tvOS**：包含安全区（safe zone）防止聚焦缩放时内容被裁剪；前景层比背景层被裁剪更多。
- **visionOS**：避免在背景层添加看起来像洞/凹面的形状（系统阴影和高光会让它反而凸显）。
- **watchOS**：避免黑色背景（图标会融入屏幕背景），把黑色调亮。

### 7.7 规格

| 平台 | 布局形状 | 遮罩后形状 | 布局尺寸 | 风格 | 外观 |
|---|---|---|---|---|---|
| iOS / iPadOS / macOS | 方形 | 圆角矩形 | 1024×1024 px | 分层 | Default、dark、clear light/dark、tinted light/dark |
| tvOS | 横向矩形 | 圆角矩形 | 800×480 px | 分层（视差） | 无 |
| visionOS | 方形 | 圆形 | 1024×1024 px | 分层（3D） | 无 |
| watchOS | 方形 | 圆形 | 1088×1088 px | 分层 | 无 |

系统自动缩放图标以产生更小变体（如设置、通知中）。支持色域：sRGB、Gray Gamma 2.2、Display P3（watchOS 除外）。

---

## 8. 无障碍（Accessibility）

为无障碍设计能触达更广泛的受众、创造更包容的体验。无障碍的界面具备三个特征：

- **直观（Intuitive）**：使用熟悉一致的交互，让任务易于完成。
- **可感知（Perceivable）**：不依赖单一方式传达信息——无论用视觉、听觉、语音还是触控，人们都能访问内容。
- **可适配（Adaptable)**：支持系统无障碍功能、允许个性化设置。

开发过程中使用 Accessibility Inspector 审查界面，发现无障碍问题、理解应用如何向使用系统无障碍功能的用户呈现；还可在 App Store 上用 Accessibility Nutrition Labels 标示应用的无障碍程度。

### 8.1 视觉（Vision）

用户可能是盲人、色觉障碍、低视力或畏光；光照和屏幕亮度也会影响交互。

- **支持更大的文字**：让人能放大文字/图标。理想情况支持放大至少 200%（watchOS 为 140%），可通过自定义 UI 或采用 Dynamic Type 实现。
- **自定义字号使用推荐默认值**（见第 4 节表格）；细字重需更大字号。
- **达到颜色对比度最低标准**：参照 WCAG 或 APCA 标准。Accessibility Inspector 采用 WCAG Level AA：

| 文字尺寸 | 字重 | 最低对比度 |
|---|---|---|
| ≤17 pt | 所有 | 4.5:1 |
| 18 pt | 所有 | 3:1 |
| 所有 | Bold | 3:1 |

若默认达不到，至少在开启"增强对比度"时提供更高对比度的配色方案；支持深色模式时两种外观都要检查。

- **优先系统颜色**：自带无障碍变体，随增强对比度、浅深外观自动适应。
- **不要仅靠颜色传达信息**：为色觉障碍用户提供形状/图标等视觉指示；考虑允许自定义配色（如图表、游戏角色）。
- **为 VoiceOver 描述界面和内容**：VoiceOver 是无需看屏幕即可体验应用的屏幕阅读器。

### 8.2 听觉（Hearing）

用户可能失聪或听力不佳，也可能身处嘈杂环境。

- **提供基于文字的媒体享受方式**，并允许自定义其视觉呈现：
  - **字幕（Captions）**：音频/视频内容中可听信息的文字等价物，适合游戏过场、视频片段。
  - **Subtitle**：让用户以偏好语言阅读屏幕上的实时对白，适合影视。
  - **音频描述（Audio descriptions）**：在主要音频的自然停顿间插入对仅视觉呈现的重要信息的口述。
  - **文字稿（Transcripts）**：完整描述视频的文字（含可听与视觉信息），适合播客、有声书。
- **用触觉（haptics）辅助音频提示**：成功提示音、错误音、游戏反馈等，为听不到声音的用户配对相应触觉；iOS/iPadOS 还可用 Music Haptics 和 Audio graphs 让音乐和图表通过振动体验。
- **用视觉提示增强音频提示**：对游戏和空间应用尤为重要——用音频引导用户行动时，同时添加指向交互位置的视觉指示。

### 8.3 肢体（Mobility）

为灵活性或活动能力受限的用户提供舒适体验。

- **提供足够大的控件**（各平台推荐尺寸见第 12 节速查表）。
- **控件间距与尺寸同样重要**：减少误触。一般规则：带边框元素周围约 12 pt 留白，无边框元素可见边缘周围约 24 pt。
- **常用交互支持简单手势**：避免自定义多指/多手手势，让重复动作舒适易记。
- **为手势提供替代方案**：核心功能应可通过多种物理交互方式达成。例如滑动关闭视图时，同时提供可点按的按钮。
- **支持 Voice Control**：让人们通过语音命令完全操作设备；确保界面元素正确标注。
- **集成 Siri 与快捷指令**：让人仅用声音执行任务、自动化重复性工作（可从 Siri、操作按钮、主屏幕/控制中心快捷指令发起）。
- **支持肢体相关的辅助技术**：VoiceOver、AssistiveTouch、Full Keyboard Access、Pointer Control、Switch Control——测试并验证应用支持这些技术且元素标注正确。

### 8.4 言语（Speech）

- **允许仅用键盘导航和交互**：开启 Full Keyboard Access 后可用物理键盘导航；避免覆盖系统定义的键盘快捷键。
- **支持 Switch Control**：让用户通过独立硬件、游戏手柄或声音（如咔哒声、爆破音）控制设备。

### 8.5 认知（Cognitive）

降低复杂度，所有人受益。

- **动作简单直观**：优先系统手势和行为，而非需要学习记忆的自定义手势。
- **尽量减少限时界面元素**：自动消失的视图对处理信息较慢、使用辅助技术的用户不友好；优先用显式动作关闭视图。
- **游戏考虑难度适配**：自定义难度、降低通关条件、调整反应时间、提供操控辅助。
- **让用户控制音视频播放**：避免不提供控制就自动播放；尊重全局的自动播放退出设置。
- **允许退出视频中的闪烁光**：响应系统的 Dim Flashing Lights 设置。
- **慎用快速移动和闪烁动画**：过度使用会分散注意、引起眩晕甚至诱发癫痫。用户开启"减弱动态效果（Reduce Motion）"时：
  - 收紧动画弹簧减少弹跳
  - 让动画直接跟随手势
  - 避免动画化 z 轴深度变化
  - 用淡入淡出代替 x/y/z 轴过渡
  - 避免动画进出模糊效果
- **为 Assistive Access 优化**（iOS/iPadOS 认知障碍用户的精简应用模式）：
  - 识别核心功能，移除非关键流程和 UI 元素
  - 拆分多步流程，每屏聚焦单一交互
  - 难以恢复的操作（如删除文件）始终二次确认

### 8.6 visionOS 特别注意事项

- **优先考虑舒适**：沉浸式特性使界面、动画、交互更容易引起晕动和视觉/人体工学不适：
  - 界面元素保持在视野内；优先水平布局（垂直布局可能引起颈部疲劳）；避免快速在多处之间争夺注意。
  - 降低动画物体的速度和强度，尤其在周边视野。
  - 相机/视频运动要温和，避免让用户感觉世界在不受控地移动。
  - 避免把内容锚定在头部（会让人感觉被困、妨碍 Pointer Control 等辅助技术）。
  - 尽量减少大幅度和重复手势的需求。

---

## 9. 菜单（Menus）

菜单在应用和游戏中无处不在，人们已经知道如何使用它们——无论系统组件还是自定义组件，人们都期待菜单以熟悉的方式运作：打开菜单显示若干菜单项，每项代表影响当前选择或上下文的命令、选项或状态。

（系统组件也内含菜单：弹出按钮/下拉按钮展示与其动作相关的选项菜单；上下文菜单提供当前视图/任务常用动作；macOS 和 iPadOS 的菜单栏包含应用的全部命令。）

### 9.1 标签（Labels）

- 菜单项标签描述其功能，必要时可配符号。应用中还可显示关联的键盘命令（游戏很少显示，因为游戏通常要处理更广范围的输入设备）。
- **清晰简洁**：发起动作的菜单项用动词或动词短语（View、Close、Select）。
- **标题式大写（title-style capitalization）**：除冠词、并列连词、短介词外每个单词首字母大写，最后一个单词无论词性都大写。
- **去掉冠词**（the、a、an）以节省空间——冠词拉长标签却很少增进理解。
- **不可用时明确显示**：不可用菜单项通常变暗且不响应；即使所有项都不可用，菜单本身必须仍可打开，让人们了解其中包含的命令。
- **需要更多信息才能完成的动作，标签后加省略号（…）**：提示需要输入或额外选择。

### 9.2 图标（Icons）

- **一致地表示常见动作**：使用系统标准图标（Share、Print、Search 等），更易用更熟悉。
- **克制且有目的地使用菜单项图标**：图标帮助更快定位、澄清动作含义。用于最常见动作、关键功能、文件系统位置、连接设备、视觉概念（旋转/翻转）和用户生成内容（文件夹/文档）；找不到清晰表意的图标就不要放。
- **同一组内视觉处理统一**：要么整组都有图标，要么都没有。

### 9.3 组织（Organization）

- **重要/常用项放最前**：人们从上往下扫描，高优先级项在前能省去通读整个菜单。
- **逻辑相关的项分组**：如编辑命令（Copy、Cut、Paste）或相机命令；用分隔线区分组。
- **相关命令保持在同一组**：即使重要性不同——如 Paste and Match Style 虽少用，人们仍期待它和 Copy、Cut 在一起。
- **注意菜单长度**：过长的菜单让人容易漏掉想要的命令——拆分为多个菜单，或用子菜单缩短列表（如把难度级别放进 New Game 的子菜单）。例外：用户定义或动态生成的菜单（如 Safari 的历史、书签）可以长、可滚动。

### 9.4 子菜单（Submenus）

菜单项可通过标签后的符号（如 chevron）表明存在子菜单。

- **克制使用**：每个子菜单都增加复杂度并隐藏其内容。当同一术语出现在同组两个以上菜单项中时考虑子菜单（如 Sort by Date/Score/Time → 用 "Sort by" 子菜单列出 Date、Score、Time）。
- **限制深度和长度**：多级子菜单难以展开，一般限制为一级；超过约五个条目时考虑新建菜单。
- **子菜单项自身保持可用**：即使内部项全部不可用，也要能打开学习其内容。
- **用子菜单而非缩进**：缩进不符合系统规范、不能清晰表达关系。

### 9.5 切换项（Toggled items）

- **用可变化标签描述当前状态**：如单个菜单项的标签在 Show Map / Hide Map 间切换，代替两个独立项。
- **标签不够清晰时加动词**：如 HDR On/Off 可能被误读为状态，改为 Turn HDR On / Turn HDR Off。
- **必要时同时显示两个菜单项**：如游戏同时列出 Take Account Online 和 Take Account Offline，当前状态对应的另一项显示为不可用。
- **用勾选标记表示属性当前生效**：便于扫描查找（如 Format > Font 菜单中标记已应用的字体样式）。
- **提供一次性移除多个切换属性的菜单项**：如 Plain 一次移除所有格式属性。

### 9.6 游戏内菜单（In-game menus）

- 让玩家用平台默认的交互方式导航游戏菜单（iOS/iPadOS 用触控，visionOS 用直接/间接手势）。
- 确保菜单在所有平台易打开、易阅读：缩放游戏内容到其他屏幕（尤其手机）可能让菜单太小——调整点按目标尺寸、考虑替代表达方式。

### 9.7 平台注意事项

- **iOS / iPadOS** 菜单支持三种布局：
  - **Small**：顶部一行 4 个仅符号无标签的项，其余项在下方列表——仅用于通常成组出现的紧密相关动作（Bold、Italic、Underline、Strikethrough）。
  - **Medium**：顶部一行 3 个符号在上、短标签在下的项——适合 3 个常做的重要动作（如 Notes 的 Scan、Lock、Pin）。
  - **Large（默认）**：所有项以列表显示。
- **visionOS**：支持 small/large 布局；菜单可从 3D 内容中用 SwiftUI 视图呈现；可用**穿透效果（breakthrough effect）**保证菜单始终可见：多数场景优先 **subtle**（保持可读性同时保留深度和情境）；**prominent** 适合必须覆盖全场的菜单但可能干扰体验；**none** 让菜单完全被 3D 内容遮挡。菜单优先显示在其控制的内容附近。

---

## 10. 输入方式、模式、组件与技术目录

HIG 还提供四大类主题的详细子页面，以下列出其完整目录，便于按需查阅。

### 10.1 输入方式（Inputs）

| 主题 | 说明 |
|---|---|
| Action button | iPhone/Apple Watch 操作按钮 |
| Apple Pencil and Scribble | 手写笔输入与随手写 |
| Camera Control | 相机控制键 |
| Digital Crown | 数字表冠 |
| Eyes | visionOS 眼动输入 |
| Focus and selection | 焦点与选择 |
| Game controls | 游戏控制器 |
| Gestures | 手势 |
| Gyroscope and accelerometer | 陀螺仪与加速度计 |
| Keyboards | 键盘 |
| Nearby interaction | 近场交互 |
| Pointing devices | 指针设备 |
| Remotes | 遥控器 |

### 10.2 模式（Patterns）

| 主题 | 说明 |
|---|---|
| Charting data | 图表数据呈现 |
| Collaboration and sharing | 协作与共享 |
| Drag and drop | 拖放 |
| Entering data | 数据录入 |
| Feedback | 反馈 |
| File management | 文件管理 |
| Going full screen | 进入全屏 |
| Launching | 启动 |
| Live-viewing apps | 实时观看类应用 |
| Loading | 加载中 |
| Managing accounts | 账户管理 |
| Managing notifications | 通知管理 |
| Modality | 模态 |
| Multitasking | 多任务 |
| Offering help | 提供帮助 |
| Onboarding | 引导入门 |
| Playing audio | 音频播放 |
| Playing haptics | 触觉播放 |
| Playing video | 视频播放 |
| Printing | 打印 |
| Ratings and reviews | 评分与评论 |
| Searching | 搜索 |
| Settings | 设置 |
| Undo and redo | 撤销与重做 |
| Workouts | 体能训练（watchOS） |

### 10.3 组件（Components）分类

- **Content**（内容）
- **Layout and organization**（布局与组织）
- **Menus and actions**（菜单与动作）
- **Navigation and search**（导航与搜索）
- **Presentation**（呈现）
- **Selection and input**（选择与输入）
- **Status**（状态）
- **System experiences**（系统体验）

### 10.4 技术（Technologies）

| 主题 | 说明 |
|---|---|
| AirPlay | 无线投屏 |
| Always On | 常亮显示 |
| App Clips | 轻应用 |
| Apple Pay | 支付 |
| Augmented reality | 增强现实 |
| CareKit / HealthKit / ResearchKit | 健康医疗框架 |
| CarPlay | 车载 |
| Game Center | 游戏中心 |
| Generative AI | 生成式 AI |
| HomeKit | 智能家居 |
| iCloud | 云存储 |
| ID Verifier | 身份核验 |
| iMessage apps and stickers | iMessage 应用与贴纸 |
| In-app purchase | 应用内购买 |
| Live Photos | 实况照片 |
| Mac Catalyst | 将 iPad 应用带到 Mac |
| Machine learning | 机器学习 |
| Maps | 地图 |
| NFC | 近场通信 |
| Photo editing | 照片编辑 |
| SharePlay | 共享播放 |
| ShazamKit | 音乐识别 |
| Sign in with Apple | 通过 Apple 登录 |
| Siri | 智能助手 |
| Tap to Pay on iPhone | iPhone 轻触支付 |
| VoiceOver | 屏幕阅读器 |
| Wallet | 钱包 |

---

## 11. UI 组件设计详解

> HIG 对每个组件都提供了「最佳实践 → 结构/内容/样式 → 平台注意事项」的完整设计指导。本章总结最常用的核心组件。

### 11.1 按钮（Buttons）

按钮兼具灵活性、高度可定制性，为人们提供简单熟悉的方式来完成任务。按钮由三个要素构成：

- **样式（Style）**：基于尺寸、颜色和形状的视觉风格
- **内容（Content）**：符号/图标、文本标签，或两者兼有
- **角色（Role）**：系统定义的语义角色，影响按钮外观

**最佳实践**：

- **给按钮足够的空间**：让按钮与周围组件在视觉上可区分；无论使用指尖、指针、眼睛还是遥控器，都必须能轻松选中。作为通用规则，按钮至少需要 **44×44 pt 的点击区域（visionOS 为 60×60 pt）**。
- **自定义按钮必须有按压状态**：没有按压状态，按钮会显得无响应。
- **用突出的视觉样式表示视图中的首选操作**：对最可能的操作使用 prominent 按钮样式，系统会给背景应用强调色。每个视图最多 **1–2 个**突出按钮——太多会加重认知负担。
- **用样式而非尺寸区分选项层级**：提供多个等尺寸按钮代表同一组选项；想突出某个选项时用更突出的样式，而不是改变尺寸。
- **不要给按钮标签应用与内容层背景相似的颜色**：内容层已色彩丰富时，按钮标签优先使用默认单色外观。

**角色（Role）**：

| 角色 | 含义 | 外观影响 |
|---|---|---|
| Normal | 无特殊含义 | — |
| Primary | 默认按钮，人们最可能选择 | 使用 App 强调色，响应 Return 键 |
| Cancel | 取消当前操作 | — |
| Destructive | 执行可能导致数据丢失的操作 | 使用系统红色 |

- 把 primary 角色分配给最可能被选择的按钮；**绝不**把 primary 角色分配给破坏性按钮——人们常不读就点突出按钮。

**平台细节**：

- **macOS** 有独有类型：
  - **Push button**（标准按钮）：打开其他窗口/视图时标题加省略号；支持 spring loading（拖拽时用力点击激活）
  - **Square button**（渐变按钮）：用于视图内的增删行等操作，只含符号不含文字
  - **Help button**：圆形、含问号；每个窗口最多一个，通常放左下角或右下角
  - **Image button**：图片与按钮边缘间约 10 像素内边距
- **visionOS**：三种标准形状——仅图标用圆形、仅文字用圆角矩形或胶囊形、图文兼有用胶囊形；优先圆形/胶囊形（视线更容易保持居中）；按钮中心间距至少 60 pt；避免白色背景黑文字的自定义按钮（系统保留给切换状态）。
- **watchOS**：内联按钮使用胶囊形状；主要操作优先全宽按钮；两个并排按钮使用相同高度。
- **iOS / iPadOS**：操作不能立即完成时，可配置按钮显示活动指示器。

### 11.2 菜单（Menus）与各类菜单组件

通用菜单的设计规范（标签、图标、组织、子菜单、切换项）已在第 9 章详述。本章补充各类菜单组件的使用场景与区别：

#### 上下文菜单（Context Menus）

- 上下文菜单默认隐藏，通过长按（iOS / iPadOS / visionOS）或 Control+点击（macOS / iPadOS）唤出。
- **优先选择相关项**：上下文菜单是让人快速访问当前上下文中最可能用到的命令，而不是放冷门项。例如 Mail 收件箱消息的上下文菜单有"回复"和"移动"，但没有"编辑内容"。
- **保持条目数量少**；全应用一致地使用上下文菜单。
- **上下文菜单中的项必须同时在主界面可用**（如工具栏或菜单栏中）。
- **不可用的项隐藏而非变暗**（与常规菜单相反）。
- **子菜单最多一层**；子菜单标题要直观可预测。
- 破坏性项放最后并用红色标识。
- **visionOS**：优先使用上下文菜单代替面板或检查器窗口，减少视图数量；菜单高度避免超过窗口高度。

#### 弹出按钮（Pop-up Buttons）

- 用于呈现**互斥的选项列表**（选择影响内容或视图）；选中后菜单关闭，按钮更新为当前选择。
- 若需列出**动作**、允许多选、含子菜单 → 用下拉按钮（pull-down button）。
- 提供有用的默认选择；用引导标签让人不打开菜单就能预测选项。
- 空间有限且不需始终展示所有选项时是好选择；可加 Custom 选项。

#### 下拉按钮（Pull-down Buttons）

- 用于呈现**与按钮动作直接相关的命令**（如 Add 按钮弹出"添加什么"菜单、Sort 按钮弹出排序依据）。
- 互斥选项用弹出按钮，命令用下拉按钮。
- **不要把一个视图的所有动作都塞进一个下拉按钮**——主要动作必须易于发现。
- 条目数量建议 ≥3（否则交互不值得），也别太多。
- 破坏性菜单项用红字标识，选中后系统要求确认（iOS 弹出操作表，iPadOS 弹出 popover）。

#### 菜单栏（The Menu Bar，macOS / iPadOS）

macOS 用户高度依赖菜单栏来学习应用功能和查找命令。标准菜单栏菜单顺序：

1. **App 名菜单**（About / Settings / Hide / Quit 等）
2. **File**（New / Open / Save / Print…）
3. **Edit**（Undo / Redo / Cut / Copy / Paste / Delete / Select All / Find…）
4. **Format**（Font / Text）
5. **View**（Show/Hide Toolbar / Sidebar / Enter Full Screen）
6. **App 专属菜单**（如 Safari 的 History、Bookmarks）
7. **Window**（Minimize / Zoom / Bring All to Front）
8. **Help**

关键规则：

- **支持系统默认的菜单和顺序**——人们期待熟悉的排列。
- **菜单项始终可见**：不可用的项禁用（变暗）而非隐藏。
- **支持标准菜单项的键盘快捷键**（Copy / Cut / Paste / Save / Print）。
- **菜单标题优先单字**。
- **iPadOS** 菜单栏在指针移到顶部或从顶部下滑时显示，居中排列；与 macOS 的差异：无 Apple 菜单、无菜单栏附加项、窗口控制在全屏时出现在菜单栏中。
- **动态菜单项**（按住修饰键改变行为，如 Option+Minimize→Minimize All）：只用于菜单快捷方式，不能作为完成任务的唯一途径，只用一个修饰键。

### 11.3 导航组件

#### 标签栏（Tab Bars）

- 标签栏用于**导航**（在应用的不同区域间切换），而非提供动作；提供动作用工具栏。
- 导航到不同区域时**保持标签栏可见**（模态视图除外）。
- **使用恰当数量的标签**：权衡复杂度与访问频率；复杂结构考虑侧边栏或可转换标签栏。
- **避免溢出标签**（iOS / iPadOS 中隐藏项会进入 More 标签，降低可发现性）。
- **不禁用、不隐藏标签按钮**：内容不可用时解释原因而非禁用。
- **使用标签文字**：尽可能单词；**使用 SF Symbols**（紧凑视图中图标在文字上方，常规视图中并排），优先填充样式。
- **徽章（badge）仅用于关键信息**：红色椭圆、白色文字/数字，不要滥用。
- 平台细节：
  - **iOS**：标签栏浮动在屏幕底部内容上方，Liquid Glass 背景；可附带迷你播放器等配件（滚动时最小化）。
  - **iPadOS**：标签栏显示在屏幕顶部附近，可固定或转换为侧边栏；优先用标签栏导航，复杂应用提供侧边栏转换选项；让用户自定义标签栏（默认 ≤5 个）。
  - **tvOS**：高度 68 pt、距顶部 46 pt，不可更改；选中项带投影；溢出项渐隐截断。
  - **visionOS**：始终垂直、浮动在窗口前缘固定位置；注视时自动展开显示标签。

#### 侧边栏（Sidebars）

- 侧边栏需要大量垂直和水平空间；空间有限时标签栏可能更合适。
- **让视觉丰富的内容延伸到侧边栏下方**（Liquid Glass 层中浮动）；可用背景延伸效果。
- **让用户自定义侧边栏内容**、使用熟悉的符号表示项目。
- **允许隐藏侧边栏**（但默认不隐藏以保证可发现性）。
- 一般**不超过两级层级**；两级时用简洁的描述性标签。
- 侧边栏图标颜色默认使用 App 强调色；固定颜色用于特殊含义（如 Mail 的 VIP 黄色图标）。
- **iOS / iPadOS**：使用 `sidebarAdaptable` 标签栏样式可同时提供侧边栏和标签栏，自动适配旋转和窗口调整；优先用标签栏，需要更多区域时用可转换侧边栏。
- **macOS**：行高/文字/字形尺寸取决于侧边栏整体大小；窗口缩小时可自动隐藏侧边栏；避免把关键信息放底部。

#### 搜索栏（Search Fields）

- 使用占位文字提示搜索范围；**尽可能在输入时即时搜索**；考虑展示搜索建议。
- **简化搜索结果**：最相关结果在前；考虑分类；考虑用范围栏（scope bar）过滤。
- 搜索入口位置（iOS）：
  - **作为标签**：标准标签样式提供发现/探索（Apple TV 用此展示分类）；按钮样式让人立即开始搜索
  - **在工具栏中**：底部优先（易于触达）；顶部用于底部有重要内容时（如 Wallet）
  - **内联**：搜索与特定内容强关联时（如 Music 资料库内的过滤）
- **iPadOS / macOS**：搜索栏通常放在工具栏尾端；过滤侧边栏/导航时放在侧边栏顶部；配丰富建议时作为侧边栏/标签栏中的独立项。

### 11.4 呈现组件

#### 警告（Alerts）

- **克制使用**：警告打断当前任务，只在提供必要信息和有用操作时使用。
- **不要仅用警告提供信息**（无操作性的信息用替代方式传达，如状态指示器）。
- **不要为常见的可撤销操作显示警告**（如删除邮件）；不常见的不可撤销操作才需要。
- **不要在应用启动时显示警告**（如网络问题，改用缓存数据+标签说明）。
- 文案：直接、中立、亲和；标题清晰简洁地描述情境（避免 "Error" 这类无信息标题）；标题是完整句子时用句首大写。
- 按钮：简洁（1–2 个词）、动词开头；"OK" 仅用于纯信息性警告；"Cancel" 永远叫 Cancel；默认按钮放尾侧/顶部，Cancel 放前缘/底部；破坏性按钮不用默认角色；有破坏性操作时必须提供 Cancel。
- 提供替代取消方式（Esc / Command-Period / 返回主屏幕 / 遥控器 Menu）。
- **visionOS**：Shared Space 中警告显示在窗口前缘；Full Space 中居中于视野；附件视图最大高度 154 pt、圆角 16 pt。

#### 操作表（Action Sheets）

- 用于**有意的动作**提供相关选择（与警告的区别：警告传达问题/意外变化）。
- 标题单行；仅在必要时提供消息；提供 Cancel 按钮（放底部）。
- **破坏性选项要视觉上突出**（destructive 样式、放顶部）。
- **iOS / iPadOS**：用操作表而非菜单提供动作相关选择；避免操作表滚动。
- **watchOS**：最多 4 个按钮（含 Cancel），即最多 3 个额外选项。

#### 表单（Sheets）

- 用于请求特定信息或呈现返回父视图前可完成的简单任务。
- macOS / tvOS / visionOS / watchOS 中始终是模态的；iOS / iPadOS 可以是模态或非模态。
- 标准按钮：Cancel（不保存关闭）/ Done（完成后关闭）/ Back（多步流程导航）。
- **复杂或长时间流程考虑替代方案**（全屏模态视图、新窗口、全屏模式、Full Space）。
- 主界面一次只显示一个表单；Done 必须搭配 Cancel 或 Back；避免同时显示三个按钮。
- **iOS / iPadOS**：
  - 单视图表单：Cancel 在顶部工具栏前缘，Done 在尾缘。
  - 可调整大小的表单有**停靠点（detents）**：large（完全展开）、medium（约一半高度）；用 grabber 指示可拖拽；支持滑动关闭。
  - iPadOS 优先使用 page 或 form sheet 呈现样式。
- **macOS**：卡片式、圆角、浮在父窗口上；父窗口变暗；人们期待能先操作其他窗口再关闭表单；需反复输入+观察结果时用面板（panel）。
- **visionOS**：浮在父窗口前缘、使其变暗；避免从窗口底部边缘升起，优先居中于视野；默认尺寸不要覆盖大部分窗口。
- **watchOS**：全屏视图，半透明、背景材质模糊去饱和；仅当需要自定义标题/内容呈现时使用；交互要简短。

#### 气泡（Popovers）

- 用于暴露**少量信息或功能**；交互后消失。
- 箭头尽可能直接指向触发它的元素；不覆盖触发元素或必要内容。
- Close 按钮仅用于确认和指导；非模态气泡自动关闭时必须保存工作。
- **一次只显示一个气泡**；不要在气泡上显示其他视图（警告除外）。
- 避免太大；尺寸变化要平滑过渡；不要用气泡显示警告（用 Alerts）。
- **iOS / iPadOS**：紧凑视图避免使用气泡，用全屏模态视图（如表单）。
- **macOS**：可让气泡可分离（拖拽变成独立面板）。

### 11.5 输入与选择组件

#### 切换（Toggles）

- 用于在**两个对立值**间选择（影响内容或视图状态）；其他类型的选择用弹出按钮等。
- 清楚标识切换影响的对象；**状态差异必须明显**（不能仅靠颜色）。
- **iOS / iPadOS**：switch 样式仅在列表行中使用（行内容提供上下文）；列表外用"表现得像切换的按钮"；默认绿色通常可行，也可用强调色（保证对比度）。
- **macOS**：
  - **Switch**：用于想强调的设置（视觉分量更大）；分组表单中用 mini switch 控制单行。
  - **Checkbox**：空=关、勾=开、破折号=混合状态；层级设置用 checkbox（对齐+缩进表达依赖）；反映混合状态。
  - **Radio button**：2–5 个一组，呈现互斥选择；>5 个选项考虑弹出按钮；单个开关设置优先 checkbox。

#### 分段控制（Segmented Controls）

- 提供**紧密相关的选项**（影响对象、状态或视图）；所有分段通常等宽。
- 可在分段控制内统一用文字或图片，不要混用。
- 分段数限制：宽界面约 5–7 个，iPhone 约 5 个。
- 标签用名词/名词短语、标题式大写。
- **iOS / iPadOS**：可用于切换紧密相关的子视图（如 Calendar 新建事件/提醒）；切换完全不同的区域用标签栏。
- **macOS**：可作单选或多选；主窗口区域切换视图用 Tab view，工具栏/检查器中切换用分段控制。
- **tvOS**：焦点移到分段上即选中（而非点击）；避免在附近放其他可聚焦元素。
- **visionOS**：注视带图标的分段控制时显示提示文字。

#### 选择器（Pickers）

- 用于选择单值或多部分值；日期选择器提供日历视图或数字键盘输入。
- 适合**中等到较长的列表**；短列表用下拉按钮，非常长的列表用列表/表格。
- 使用**可预测、逻辑排序**的值（如按字母排序的国家列表）。
- 避免切换视图来显示选择器——应在上下文中显示（字段下方或附近）。
- 日期选择器样式（iOS / iPadOS）：Compact（按钮+模态视图）/ Inline / Wheels / Automatic；模式：Date / Time / Date and time / Countdown timer。
- **macOS**：文本型（空间有限）或图形型（日历浏览/日期范围/表盘风格）。
- **watchOS**：用 Digital Crown 导航。

#### 滑块（Sliders）

- 值变化时，最小值与拇指之间的轨道填充颜色；可配左右图标说明含义。
- **方向要熟悉**：水平滑块最小值在前缘、最大值在尾缘；垂直滑块最小值在底部。
- 可搭配文本框和步进器补充精确输入。
- **iOS / iPadOS**：不要用滑块调节音量（用系统音量视图）。
- **macOS**：支持刻度线；线性滑块适合固定起止点，圆形滑块适合循环/无限值（如旋转角度）；提供实时反馈；标签用句首大写+冒号结尾。
- **visionOS**：优先水平滑块。
- **watchOS**：水平轨道，两侧加减按钮；默认显示加减号，必要时自定义字形。

#### 步进器（Steppers）

- 本身不显示值，放在显示当前值的字段旁。
- 让步进器影响的值显而易见；大值变化时搭配文本框。
- **macOS**：支持 Shift+点击快速变化（如 10 倍默认增量）。

#### 文本字段（Text Fields）

- 用于请求**少量信息**（姓名、邮箱）；大量文本用文本视图。
- 用占位文字提示用途；敏感数据用安全文本字段；尺寸匹配预期文本量。
- 多个字段均匀间隔、垂直堆叠、宽度一致；Tab 切换按逻辑顺序。
- 适当时机验证字段（邮箱在切换字段时验证，密码在切换前验证）。
- 数字数据用数字格式化器；根据字段需求调整换行/截断方式。
- **iOS / iPadOS**：尾端提供清除按钮；前端指示用途、尾端提供附加功能。
- **macOS**：文本输入+选项列表用组合框（combo box）。
- **watchOS / tvOS**：最小化文本输入，优先列表选项或按钮。

#### 标签（Labels）

- 显示不可编辑的文本（按钮内、列表项、视图说明）。
- 少量不可编辑文本用标签；可编辑少量文本用文本字段；大量文本用文本视图。
- 优先系统字体（自动支持 Dynamic Type）。
- 用系统标签颜色表达相对重要性：Label（主要）/ Secondary（副标题）/ Tertiary（不可用项）/ Quaternary（水印）。
- 有用的标签文字（错误信息、位置、IP 地址）允许选择复制。

#### 列表与表格（Lists and Tables）

- 行式格式特别适合文本浏览；尺寸差异大或图片多时考虑集合（collection）。
- 允许有意义的编辑（重排序等）；选中项提供恰当反馈。
- 条目文字简洁；多列表格用描述性列标题（名词/名词短语、标题式大写、无结尾标点）。
- 选择与数据和平台协调的表格样式（iOS / iPadOS 分组样式、watchOS 椭圆样式、macOS 交替行背景样式）。
- **macOS**：点击列标题排序（再次点击反向）；允许调整列宽；多列表格考虑交替行颜色；层级数据用大纲视图。
- **tvOS**：聚焦行会放大并圆角，准备图片时考虑此效果。
- **watchOS**：限制行数；长列表列出最相关项并提供查看更多方式。

### 11.6 状态组件

#### 进度指示器（Progress Indicators）

- 两种类型：**确定型**（已知时长，如文件转换——填充线性或圆形轨道）和**不确定型**（无法量化，如加载/同步——动画指示器/转圈）。
- **尽可能用确定型**——帮人们判断是等待、稍后重试还是放弃。
- **报告进度要准确**：避免"5 秒到 90%，最后 10% 花 5 分钟"。
- **保持指示器移动**：静止的指示器让人觉得进程卡死或应用冻结。
- 尽可能从不确定型切换为确定型；**不要在圆形和条形样式间切换**。
- 显示一致的位置；可行时允许取消（提供 Cancel 按钮）；取消有负面后果时提供警告确认。
- **iOS / iPadOS 刷新控件**：拖拽触发的专用活动指示器；同时执行自动内容更新；标题仅在有价值时提供。
- **macOS**：不确定型可用条形或圆形；后台操作/空间受限优先转圈；转圈不加标签。

#### 量表（Gauges）

- 用圆形或线性路径表示值的范围；标准样式显示当前位置指示器，capacity 样式填充到当前位置。
- 有 accessory 变体（视觉上类似 watchOS 复杂功能），适合 iOS 锁屏小组件。
- 标签简洁地描述当前值和范围两端；用渐变填充路径帮助传达用途（如温度用红→蓝）。
- **macOS** 另有 level indicator：capacity（连续/离散值，大范围用连续样式）、rating（评分）、relevance（相关度）；可在值达到特定水平时改变填充颜色（默认绿色）。

### 11.7 组件选择速查表

| 需求场景 | 推荐组件 |
|---|---|
| 触发一个动作 | 按钮 |
| 从互斥选项中选一个 | 弹出按钮 / 分段控制 / 单选按钮 |
| 从互斥选项中选一个（命令类） | 下拉按钮 |
| 切换开/关状态 | 切换（switch / checkbox） |
| 展示少量相关选项 | 分段控制 |
| 展示中等长度列表 | 选择器 |
| 展示长列表 | 列表/表格 |
| 输入少量文本 | 文本字段 |
| 输入大量文本 | 文本视图 |
| 连续值调整 | 滑块 |
| 离散步进调整 | 步进器 |
| 当前上下文相关的常用命令 | 上下文菜单 |
| 与动作直接相关的选项 | 下拉按钮 |
| 互斥选项影响内容 | 弹出按钮 |
| 导航到应用不同区域 | 标签栏 |
| 复杂层级导航 | 侧边栏 + 分栏视图 |
| 当前视图的动作 | 工具栏 |
| 意外问题/破坏性确认 | 警告 |
| 有意动作的相关选择 | 操作表 |
| 请求信息/完成简单任务 | 表单 |
| 临时少量信息/功能 | 气泡 |
| 已知时长进度 | 确定型进度指示器 |
| 未知时长进度 | 不确定型进度指示器 |
| 显示范围内的当前值 | 量表 |

---

## 12. 关键规格速查表

### 12.1 默认/最小文字尺寸

| 平台 | 默认 | 最小 |
|---|---|---|
| iOS / iPadOS | 17 pt | 11 pt |
| macOS | 13 pt | 10 pt |
| tvOS | 29 pt | 23 pt |
| visionOS | 17 pt | 12 pt |
| watchOS | 16 pt | 12 pt |

### 12.2 控件尺寸（无障碍推荐）

| 平台 | 默认控件尺寸 | 最小控件尺寸 |
|---|---|---|
| iOS / iPadOS | 44×44 pt | 28×28 pt |
| macOS | 28×28 pt | 20×20 pt |
| tvOS | 66×66 pt | 56×56 pt |
| visionOS | 60×60 pt | 28×28 pt |
| watchOS | 44×44 pt | 28×28 pt |

控件间距参考：带边框元素周围约 12 pt；无边框元素可见边缘周围约 24 pt。

### 12.3 颜色对比度（WCAG AA）

| 文字尺寸 | 字重 | 最低对比度 |
|---|---|---|
| ≤17 pt | 所有 | 4.5:1 |
| 18 pt | 所有 | 3:1 |
| 所有 | Bold | 3:1 |

### 12.4 App 图标规格

| 平台 | 布局尺寸 | 遮罩后形状 | 外观 |
|---|---|---|---|
| iOS / iPadOS / macOS | 1024×1024 px | 圆角矩形 | Default / dark / clear / tinted |
| tvOS | 800×480 px | 圆角矩形（视差分层） | — |
| visionOS | 1024×1024 px | 圆形 | — |
| watchOS | 1088×1088 px | 圆形 | — |

### 12.5 安全区参考

- **tvOS**：主要内容距上下边缘 60 pt、距左右 80 pt。
- **visionOS**：按钮中心间距至少 60 pt；窗口边界外是系统控件区域。
- **iOS / iPadOS / macOS**：尊重系统 safe area，避开灵动岛、传感器壳、摄像头凸台等。

---

## 附：核心要点浓缩

1. **设计以人本**：八大原则（目的、自主、责任、熟悉感、灵活、简洁、匠心、愉悦）是一切决策的基石。
2. **拥抱系统能力**：优先系统颜色、文本样式、控件、材质和图标——它们自动适配外观、无障碍和平台演进。
3. **Liquid Glass 是当前设计语言的核心**：它是控件/导航的功能层材质，不要用于内容层；克制使用、尊重其 regular/clear 变体的使用场景。
4. **适配是硬要求**：深浅色、增强对比度、Dynamic Type、RTL、多设备尺寸方向、安全区，全部必须覆盖并实际测试。
5. **无障碍不是可选项**：可感知、可操作、可理解——从字号对比度、手势替代方案到认知负担控制，贯穿每个维度。
6. **每个平台一视同仁**：同一款应用在每个支持的平台都应感觉"像原生"，投入同等程度的打磨。

---

*本文档基于 Apple Human Interface Guidelines 官方内容整理（2026-09 抓取）。完整原文请访问：*
<https://developer.apple.com/cn/design/human-interface-guidelines/>
*相关资源：Apple Design Resources（模板与网格）、Icon Composer（图标设计工具）、SF Symbols（符号库）。*
